#include <stdio.h>

/**
* main - A function that show some numbers
*
* Return: Returns the numbers from 0 to 9
*/


int main(void)
{
int num;

for (num = 0 ; num < 10 ; num++)
{
printf("%d", num);

}
putchar('\n');
return (0);
}

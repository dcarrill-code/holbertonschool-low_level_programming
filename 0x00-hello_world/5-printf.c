#include <stdio.h>
/**
* main - This main show some text too
* Description: This C code show some text that i need whit p* rintf
* Return: Returns a string whit the text needed
*/

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");

return (0);
}
